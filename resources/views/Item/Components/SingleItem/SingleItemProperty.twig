<div id="accordion" class="single-overview mt-5">
    {# Material & Qualität #}
    <div class="card">
        <div class="card-header" id="headingOne">
            <h2 class="mb-0">
                <button class="d-flex align-items-center justify-content-between btn btn-lg btn-block btn-link"
                        data-toggle="collapse" data-target="#collapseOne"
                        aria-expanded="true" aria-controls="collapseOne">
                    {{ trans("devMlt::SingleItem.SingleItemProperty1") }}
                </button>
            </h2>
        </div>
        <div id="collapseOne" class="collapse show"
             aria-labelledby="headingOne">
            <div class="card-body">
                <dl v-for="variationProperty in currentVariation.variationProperties"
                    v-if="variationProperty.id == 30">
                    <dt class="w-50 pull-left font-weight-bold"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 345 && subProperty.values.value"
                    >
                        <div>
                            {{ trans("devMlt::SingleItem.SingleItemProperty2") }}
                        </div>
                    </dt>
                    <dd class="w-50 pull-left"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 345 && subProperty.values.value">
                        <div class="d-flex">
                            ${ subProperty.values.value }
                            <div v-if="subProperty.values.description" class="ml-2">
                                <a href="javascript:void(0)" class="child_desc_info">
                                    <i aria-hidden="true" data-toggle="tooltip" data-placement="top" height="120"
                                       :data-original-title="subProperty.values.description"
                                       class="fa fa-info-circle"></i>
                                </a>
                            </div>
                            <div v-else>
                            </div>
                        </div>
                    </dd>
                    <dt class="w-50 pull-left font-weight-bold"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 346 && subProperty.values.value">
                        <div>
                            {{ trans("devMlt::SingleItem.SingleItemProperty3") }}
                        </div>
                    </dt>
                    <dd class="w-50 pull-left"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 346 && subProperty.values.value">
                        <div>

                            <div v-if="subProperty.values.value">
                                ${ subProperty.values.value }
                            </div>
                            <div v-else>
                            </div>
                        </div>
                    </dd>
                    <dt class="w-50 pull-left font-weight-bold"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 633 && subProperty.values.value">
                        <div>
                            {{ trans("devMlt::SingleItem.SingleItemProperty4") }}
                        </div>
                    </dt>
                    <dd class="w-50 pull-left"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 633 && subProperty.values.value">
                        <div>
                            <div v-if="subProperty.values.value">
                                ${ subProperty.values.value }
                            </div>
                            <div v-else>
                            </div>
                        </div>
                    </dd>
                    <dt class="w-50 pull-left font-weight-bold"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 678 && subProperty.values.value">
                        <div>
                            {{ trans("devMlt::SingleItem.SingleItemProperty5") }}
                        </div>
                    </dt>
                    <dd class="w-50 pull-left"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 678 && subProperty.values.value">
                        <div>
                            <div v-if="subProperty.values.value">
                                ${ subProperty.values.value }
                            </div>
                            <div v-else>
                                <div v-for="eachvalue in subProperty.values">
                                    ${ eachvalue.value }
                                </div>
                            </div>

                            <div v-else>
                            </div>
                        </div>
                    </dd>
                    <dt class="w-50 pull-left font-weight-bold"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 348 && subProperty.values.value">
                        <div>
                            {{ trans("devMlt::SingleItem.SingleItemProperty6") }}
                        </div>
                    </dt>
                    <dd class="w-50 pull-left"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 348 && subProperty.values.value">
                        <div class="d-flex">
                            <div v-if="subProperty.values.value">
                                ${ subProperty.values.value }
                            </div>
                            <div v-if="subProperty.names.description" class="ml-2">
                                <a href="javascript:void(0)" class="child_desc_info">
                                    <i aria-hidden="true" data-toggle="tooltip" data-placement="top" height="120"
                                       :data-original-title="subProperty.names.description"
                                       class="fa fa-info-circle"></i>
                                </a>
                            </div>
                            <div v-else>
                            </div>
                        </div>
                    </dd>
                    <dt class="w-50 pull-left font-weight-bold"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 349 && subProperty.values.value">
                        <div>
                            {{ trans("devMlt::SingleItem.SingleItemProperty7") }}
                        </div>
                    </dt>
                    <dd class="w-50 pull-left"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 349 && subProperty.values.value">
                        <div>

                            <div v-if="subProperty.values.value">
                                ${ subProperty.values.value }
                            </div>
                            <div v-else>
                            </div>
                        </div>
                    </dd>
                    <dt class="w-50 pull-left font-weight-bold"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 350 && subProperty.values.value">
                        <div>
                            {{ trans("devMlt::SingleItem.SingleItemProperty8") }}
                        </div>
                    </dt>
                    <dd class="w-50 pull-left"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 350 && subProperty.values.value">
                        <div>
                            <div v-if="subProperty.values.value">
                                ${ subProperty.values.value }
                            </div>
                            <div v-else>
                            </div>
                        </div>
                    </dd>
                    <dt class="w-50 pull-left font-weight-bold"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 351 && subProperty.values.value">
                        <div>
                            {{ trans("devMlt::SingleItem.SingleItemProperty9") }}
                        </div>
                    </dt>
                    <dd class="w-50 pull-left"
                        v-for="subProperty in variationProperty.properties"
                        v-if="subProperty.id == 351 && subProperty.values.value">
                        <div>
                            <div v-if="subProperty.values.value">
                                ${ subProperty.values.value }
                            </div>
                            <div v-else>
                            </div>
                        </div>
                    </dd>
                </dl>
            </div>
        </div>
    </div>


    {# maße und Gewicht #}

    <div class="card">
        <div class="card-header" id="headingTwo">
            <h2 class="mb-0">
                <button class="d-flex align-items-center justify-content-between btn btn-lg btn-block btn-link collapsed"
                        data-toggle="collapse" data-target="#collapseTwo"
                        aria-expanded="false" aria-controls="collapseTwo">
                    {{ trans("devMlt::SingleItem.SingleItemProperty10") }}
                </button>
            </h2>
        </div>
        <div id="collapseTwo" class="collapse" aria-labelledby="headingTwo">
            <div class="card-body">
                <div class="d-flex ">
                    <dl class="w-100" v-for="variationProperty in currentVariation.variationProperties"
                        v-if="variationProperty.id == 30">
                        <dt class="w-50 pull-left font-weight-bold"
                            v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 657 && subProperty.values.value"
                        >
                            <div>
                                {{ trans("devMlt::SingleItem.SingleItemProperty11") }}
                            </div>
                        </dt>
                        <dd class="w-50 pull-left"
                            v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 657 && subProperty.values.value">
                            <div>
                                <div v-if="subProperty.values.value">
                                    ${ subProperty.values.value }
                                </div>
                                <div v-else>
                                </div>
                            </div>
                        </dd>
                        <dt class="w-50 pull-left font-weight-bold"
                            v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 658 && subProperty.values.value">
                            <div>
                                {{ trans("devMlt::SingleItem.SingleItemProperty12") }}
                            </div>
                        </dt>
                        <dd class="w-50 pull-left"
                            v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 658 && subProperty.values.value">
                            <div>
                                <div v-if="subProperty.values.value">
                                    ${ subProperty.values.value }
                                </div>
                                <div v-else>
                                </div>
                            </div>
                        </dd>
                        <dt class="w-50 pull-left font-weight-bold"
                            v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 659 && subProperty.values.value">
                            <div>
                                {{ trans("devMlt::SingleItem.SingleItemProperty13") }}
                            </div>
                        </dt>
                        <dd class="w-50 pull-left"
                            v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 659 && subProperty.values.value">
                            <div class="d-flex">
                                <div v-if="subProperty.values.value">
                                    ${ subProperty.values.value }
                                </div>
                                <div v-if="subProperty.values.description" class="ml-2">
                                    <a href="javascript:void(0)" class="child_desc_info">
                                        <i aria-hidden="true" data-toggle="tooltip" data-placement="top" height="120"
                                           :data-original-title="subProperty.values.description"
                                           class="fa fa-info-circle"></i>
                                    </a>
                                </div>
                                <div v-else>
                                </div>
                            </div>
                        </dd>
                    </dl>
                </div>
            </div>
        </div>
    </div>


    {# Mehr zu diesem Produkt #}
    <div class="card">
        <div class="card-header" id="headingThree">
            <h2 class="mb-0">
                <button class="d-flex align-items-center justify-content-between btn btn-lg btn-block btn-link collapsed"
                        data-toggle="collapse" data-target="#collapseThree"
                        aria-expanded="false" aria-controls="collapseThree">
                    {{ trans("devMlt::SingleItem.SingleItemProperty14") }}
                </button>
            </h2>
        </div>
        <div id="collapseThree" class="collapse" aria-labelledby="headingThree">
            <div class="card-body">
                <div class="d-flex justify-content-start pl-3">
                    <ul class="w-100 px-0" v-for="variationProperty in currentVariation.variationProperties"
                        v-if="variationProperty.id == 30">
                        <li v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 660 && subProperty.values.value">
                            <div>
                                <div v-if="subProperty.values.value">
                                    ${ subProperty.values.value }
                                </div>
                                <div v-else>
                                </div>
                            </div>
                        </li>
                        <li v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 661 && subProperty.values.value">
                            <div>
                                <div v-if="subProperty.values.value">
                                    ${ subProperty.values.value }
                                </div>
                                <div v-else>
                                </div>
                            </div>
                        </li>
                        <li v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 662 && subProperty.values.value">
                            <div>
                                <div v-if="subProperty.values.value">
                                    ${ subProperty.values.value }
                                </div>
                                <div v-else>
                                </div>
                            </div>
                        </li>

                        <li v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 663 && subProperty.values.value">
                            <div>
                                <div v-if="subProperty.values.value">
                                    ${ subProperty.values.value }
                                </div>
                                <div v-else>
                                </div>
                            </div>
                        </li>
                        <li v-for="subProperty in variationProperty.properties"
                            v-if="subProperty.id == 664 && subProperty.values.value">
                            <div>
                                <div v-if="subProperty.values.value">
                                    ${ subProperty.values.value }
                                </div>
                                <div v-else>
                                </div>
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-header" id="headingThree">
            <h2 class="mb-0">
                <button class="d-flex align-items-center justify-content-between btn btn-lg btn-block btn-link collapsed"
                        data-toggle="collapse" data-target="#collapseFour"
                        aria-expanded="false" aria-controls="collapseFour">
                    {{ trans("devMlt::SingleItem.SingleItemProperty15") }}
                </button>
            </h2>
        </div>
        <div id="collapseFour" class="collapse"
             aria-labelledby="headingFour">
            <div class="card-body">
                <a href="javascript:void(0)" class="img-desc">
                    <img class="img-fluid"
                         src="https://cdn02.plentymarkets.com/6wg68w8zahno/frontend/Webshop-Neu/Startseiten_Banner/Soziales/Gute_Projekte.jpg"
                         style="max-width: 20em">
                    <p>
                        {{ trans("devMlt::SingleItem.NachhaltigkeitText") }}
                    </p>
                </a>
            </div>
        </div>
        <p class="artikel_nummer">{{ trans("devMlt::SingleItem.ArtikelNummer") }}: ${currentVariation.variation.id }</p>
    </div>
</div>